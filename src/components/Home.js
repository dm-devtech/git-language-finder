import React, { Component } from 'react';
import { Link } from 'react-router-dom';

const Home = ({props}) =>
   (
    <div>
     <h1>Git Hub Language Finder</h1>
    </div>
    );

export default Home;
